// stdafx.cpp : 只包括标准包含文件的源文件
// PlaneGame.pch 将作为预编译头
// stdafx.obj 将包含预编译类型信息

#include "stdafx.h"

//所谓头文件预编译，就是把一个工程(Project)中使用的
//一些MFC标准头文件(如Windows.H、Afxwin.H)预先编译，
//以后该工程编译时，不再编译这部分头文件，仅仅使用预编译的结果。
//这样可以加快编译速度，节省时间。
